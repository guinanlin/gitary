{
  "name": "@gitary/electron",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/main/main.js",
  "scripts": {
    "dev": "tsc && electron .",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist",
    "generate-icons": "node scripts/generate-icons-auto.js"
  },
  "dependencies": {
    "electron": "^28.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.5.6",
    "electron-builder": "^24.9.1",
    "electron-icon-maker": "^0.0.5",
    "typescript": "^5.0.2"
  }
}
